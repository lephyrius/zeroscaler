[package]
name = "boot-fargate"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-config = "1"
aws-sdk-elasticloadbalancingv2 = "1"


[dev-dependencies]
tokio = { version = "1", features = ["full"] }
mockall = "*"